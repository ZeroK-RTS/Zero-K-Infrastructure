<Window x:Class="MissionEditor2.StringRequest" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="String Request" Height="91" WindowStyle="ToolWindow" Width="496"
    WindowStartupLocation="CenterScreen" FocusManager.FocusedElement="{Binding ElementName=TextBox}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition />
    </Grid.RowDefinitions>
    <TextBox Name="TextBox" x:FieldModifier="public" Loaded="TextBox_Loaded" />
    <UniformGrid Grid.Row="1" Rows="1" HorizontalAlignment="Right">
      <Button Margin="2" IsDefault="True" Click="Button_Click">OK</Button>
      <Button Margin="2" IsCancel="True">Cancel</Button>
    </UniformGrid>
  </Grid>
</Window>
