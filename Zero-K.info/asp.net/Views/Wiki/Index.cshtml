@model ZeroKWeb.Controllers.WikiController.WikiData
@{
    Page.Title = "Zero-K wiki";
}
<!php doesn't render -- these should be different than main page anyway>
@Html.IncludeFile("_sidebar.inc")
@Html.IncludeFile("_content.inc")

<!hacks, body should probably be cleaned instead>
<div class=""><!wierd intro sentence>
<div>
@Model.Content
<!need to remove ending break as well>

