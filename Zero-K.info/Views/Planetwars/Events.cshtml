@using ZkData
@using ZeroKWeb
@model ZeroKWeb.Controllers.EventsResult
@if (Model.Partial) {
  Layout = null;
} else {
  Page.Title = "PlanetWars events";
}
@using (Ajax.BeginForm("Events", new { accountID = Model.AccountID, clanID = Model.ClanID, planetID = Model.PlanetID, springBattleID = Model.SpringBattleID, partial = Model.Partial }, new AjaxOptions() { InsertionMode = InsertionMode.Replace, UpdateTargetId = "events", LoadingElementId = "ajaxScrollProgress" })) {
  <div id='events'>
    @if (!Model.Partial) {
      <h3>
        PlanetWars events</h3>
    }
    <small>
      @Html.Hidden("page", Model.Page, new { id = "ev_page" })
      <table cellpadding='0' cellspacing='0'>
        <th>
          Time
        </th>
        <th>
          Event
        </th>
        @foreach (var e in Model.Events) {
          <tr>
            <td>@e.Time.ToAgoString()
            </td>
            <td>@Html.Raw(e.Text)
            </td>
          </tr>
        }
        <tr>
          <td>
            @if (Model.Page > 0) {
              <a href="#" onclick="$('#ev_page').val(parseInt($('#ev_page').val())-1); $('#ev_submit').click()">
                newer</a>
            }
            <span>&nbsp;|&nbsp</span>
            @if (Model.Page < Model.PageCount - 1) {
              <a href="#" onclick="$('#ev_page').val(parseInt($('#ev_page').val())+ 1); $('#ev_submit').click()">
                older</a>
            }
          </td>
          <td align="right">
          Page @(Model.Page+1) / @Model.PageCount <a href='@Url.Action("Events", "Planetwars", new { accountID = Model.AccountID, clanID = Model.ClanID, planetID = Model.PlanetID, springBattleID = Model.SpringBattleID })'>all</a> | <a href='@Url.Action("Events", "Planetwars")'>unfiltered</a>
            @Html.TextBox("filter", Model.Filter)<input type="submit" value="filter" id="ev_submit" />
            
          </td>
        </tr>
      </table>
    </small>
  </div>
}
